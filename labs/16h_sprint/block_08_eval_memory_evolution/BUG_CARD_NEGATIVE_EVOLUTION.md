# Bug Card: 省 Token 的负提升被晋升

## 现象

候选 Prompt 少用 30% Token，但任务成功率明显下降，演进控制器仍将其晋升。

## 不变量

Eval gate 必须先满足质量、安全和关键场景阈值，再比较成本；缺失指标按失败处理，权限扩张和 Hard Policy 修改直接拒绝。

## 诊断提示

先写清 metric direction、绝对阈值、相对退化上限和必测场景，再看聚合逻辑是否被成本指标“平均掉”。
