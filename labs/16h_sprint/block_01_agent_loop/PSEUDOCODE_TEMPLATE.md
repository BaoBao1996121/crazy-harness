# Block 1 伪代码模板

先不要看 `AgentLoop` 实现，按运行结果补全：

```text
function run_one_turn(observation):
    context = ______________________________
    response = _____________________________
    persist(_______________________________)
    command = ______________________________
    validate(_______________________________)
    authorize(______________________________)
    result = _______________________________
    persist(_______________________________)
    return _________________________________
```

## 三个崩溃点

| 崩溃位置 | 最后一个可信事实 | 朴素版本会重复什么 | known-good 应怎样恢复 |
|---|---|---|---|
| 模型返回前 |  |  |  |
| `model.completed` 落盘后 |  |  |  |
| 工具效果发生、结果未落盘 |  |  |  |
